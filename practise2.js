// 1.
const dividedBy5 = num => num / 5;
console.log(dividedBy5(59));

// 2.
const math = (x, y) => {
    const sum1 = x + 2;
    const sum2 = y + 2;
    const product = sum1 * sum2;
    return product
}
console.log(math(2, 5));

// 5.
const maths = (x, y) => (x + 2) * (y + 2);
console.log(maths(2, 5))

// 4.
const product = (x, y, z) => x * y * z;
console.log(product(2, 4, 5));

